{
  "name": "anpus-feather-ios",
  "private": true,
  "version": "2.0.0",
  "description": "Anpu's Feather — native iOS app (Capacitor wrapper around the web experience)",
  "scripts": {
    "build:web": "npm --prefix ../web run build",
    "sync": "npm run build:web && cap sync ios",
    "open": "cap open ios",
    "add:ios": "cap add ios"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/ios": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}
